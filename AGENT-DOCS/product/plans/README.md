# Plans

Use this folder for `PLAN-*` implementation plans and task handoff briefs.

Plans own:

- intended approach
- changed surfaces
- sequencing
- parallelization boundaries for subagents
- validation strategy
- rollback or risk notes

Create a plan when work spans multiple files, has uncertain ordering, or needs
delegation. Small direct fixes can be documented in a session log instead.
