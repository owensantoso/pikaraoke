# Repo Health Plans

Use this folder for `PLAN-*` parent plans that improve documentation, workflow,
testing, CI, maintainability, or agent coordination.

Plan folders use:

```text
PLAN-####-slug/
  PLAN-####-slug.md
```

Use `IMPL-*` briefs inside the same plan folder only when work needs bounded
delegation or resumability.
